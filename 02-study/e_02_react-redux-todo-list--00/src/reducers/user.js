/* eslint-disable default-param-last */
const initialState = {
  name: 'milkmidi',
  permission: 'ADMIN',
};

export default function user(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
